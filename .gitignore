node_modules/
dist/
.env
.env.*
*.local
.DS_Store
Thumbs.db

# plans/docs — local only, never pushed to git.
# Blanket rules: no Markdown, no HTML, no plain-text notes in the repo.
# Only code (.js/.mjs) and manifest.json are tracked.
*.md
*.html
*.txt
research/
docs/
st-chatu8/
st_chatu8_*.json

# Claude Code agent worktrees — local only
.claude/
